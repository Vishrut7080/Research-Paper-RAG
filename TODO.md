# TODO — Out-of-Scope / Future Work

Items intentionally cut from the current implementation round. Each maps to a checklist from the ResearchMate plan. Pick them up later; none block the current core (RAG + web search + backend server + React UI + database).

---

## 1. Topic Suggestions (`GET /suggest-topics`)
- [ ] `backend/rag/topic_suggester.py` — extract key topics from corpus using Groq/Claude
- [ ] Guard: only suggest when corpus > 20 documents
- [ ] `GET /suggest-topics` endpoint in `backend/main.py`
- [ ] `frontend/src/components/TopicSuggestions.jsx` + sidebar button

## 2. Export Chat as Markdown
- [ ] `exportAsMarkdown()` in `ChatInterface.jsx` — generate `.md` from messages (Q/A + sources)
- [ ] Download blob button: `chat-history.md`
- [ ] Optional `GET /export` backend variant

## 3. Dark Mode Toggle
- [ ] Tailwind `dark:` classes in components
- [ ] Toggle button persisted to `localStorage`

## 4. Evaluation Harness & Submission Prep
- [ ] `backend/rag/evaluator.py` — retrieval accuracy@k over `test_queries.json` (15 queries + expected_sources)
- [ ] Baselines: BM25 (`rank-bm25`) and zero-shot (no retrieval)
- [ ] `notebooks/evaluation.ipynb` with baseline bar chart, per-query breakdown, response-time histogram
- [ ] `requirements.txt` finalized, fresh-clone verification script
- [ ] Presentation rehearsal + GitHub/Moodle submission checklist

---

## Also Deferred (from the full checklist)
- [ ] `/documents/{doc_id}` DELETE endpoint
- [ ] Batch PDF upload
- [ ] Semantic reranking (re-rank top-10 with LLM)
- [ ] Multi-hop retrieval
- [ ] User feedback loop (helpful/unhelpful markers)
- [ ] Fine-tuning embeddings (needs 500+ examples; skipped intentionally)
- [ ] Deployment hardening: auth, rate limiting, logging, monitoring

---

*Generated by agent during implementation. Items above are the "out of scope" set referenced in the build plan.*